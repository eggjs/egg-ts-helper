sudo: false
language: node_js
node_js:
  - '8'
  - '10'
install:
  - npm i npminstall && npminstall
os:
  - osx
  - linux
  - windows
script:
  - npm run cov
after_script: "npm install coveralls && cat ./coverage/lcov.info | coveralls"
